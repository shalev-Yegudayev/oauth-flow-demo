server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — routes not matching a real file serve index.html
    # so React Router can handle them client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
